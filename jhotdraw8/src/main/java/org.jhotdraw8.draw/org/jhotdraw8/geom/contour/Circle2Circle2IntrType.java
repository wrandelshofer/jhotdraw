package org.jhotdraw8.geom.contour;

public enum Circle2Circle2IntrType {
    // no intersect between circles
    NoIntersect,
    // one intersect between circles (tangent)
    OneIntersect,
    // two intersects between circles
    TwoIntersects,
    // circles are coincident
    Coincident
}
