<?xml version="1.0" encoding="UTF-8"?>
<!-- Builds the "JHotDraw" jar file. -->
<project name="JHotDraw" default="all" basedir=".">
    <target name="init">
        <!-- gobal build properties -->
        <property file="build.properties"/>
        
        <!-- properties for copyright and versioning -->
        <property name="name" value="${ant.project.name}"/>

        <!-- build properties -->
        <property name="lib" value="lib"/>
        <property name="src" value="src/main/java"/>
        <property name="build" value="build"/>
        <property name="classes" value="${build}/classes"/>
        <property name="dist" value="dist/JHotDraw ${version}"/>
        <property name="javadoc" value="dist/JHotDraw ${version}/JavaDoc"/>
        <property name="doc" value="dist/JHotDraw ${version}/Documentation"/>
        <property name="docsrc" value="src/documentation"/>
    </target>
    <target name="clean" depends="init">
        <delete dir="${classes}" />
    </target>
    <target depends="init" description="Javadoc for my API." name="javadoc">
        <mkdir dir="${javadoc}"/>
        <javadoc destdir="${javadoc}" 
        footer="&lt;font size=-2>Copyright (c) ${author}.&lt;br>${license}.&lt;/font>"
        header="JHotDraw ${version}" 
        windowtitle="JHotDraw ${version}"
        maxmemory="256M"
        overview="${src}/org/jhotdraw/overview.html"
        charset="UTF-8"
        >
            <packageset dir="${src}">
                <include name="org/jhotdraw/**"/>
            </packageset>
            <sourcepath>
                <pathelement location="${src}"/>
            </sourcepath>
            <classpath>
            </classpath>
        </javadoc>
    </target>
    <target name="compile" depends="init,clean">
        <mkdir dir="${classes}"/>
        <javac  destdir="${classes}"
                modulesourcepath="${src}"
                
                debug="${javac.debug}"
                optimize="${javac.optimize}"
                encoding="${javac.encoding}"
                includeantruntime="${javac.includeantruntime}"
                source="${javac.source}"
                target="${javac.target}"
                bootclasspath="${javac.bootclasspath}"
                classpath="${javac.classpath}"
         >
            <compilerarg value="-version"/>
            <compilerarg value="-Xlint:all"/>
            <include name="**/*.java"/>
        </javac>
        <copy todir="${classes}">
            <fileset dir="${src}">
                <include name="**/*.properties"/>
                <include name="**/*.png"/>
            </fileset>
        </copy>
    </target>

    <macrodef name="modular-jar">
        <attribute name="module" />
        <sequential>
            <mkdir dir="${dist}"/>
            <jar destfile="${dist}/@{module}.jar"
                 basedir="${classes}/@{module}"
            >
                <manifest>
                    <attribute name="Copyright" value="${author}"/>
                    <attribute name="License" value="${license}"/>
                    <attribute name="Implementation-Title" value="@{module}"/>
                    <attribute name="Implementation-Version" value="${version}"/>
                    <attribute name="Implementation-Vendor" value="${author}"/>
                    <attribute name="Specification-Title" value="@{module}"/>
                    <attribute name="Specification-Version" value="${version}"/>
                    <attribute name="Specification-Vendor" value="${author}"/>
                </manifest>
            </jar>
        </sequential>
    </macrodef>

    <target name="jar" depends="init,compile">
        <modular-jar module="javax.annotation"/>
        <modular-jar module="jhotdraw.application"/>
        <modular-jar module="jhotdraw.draw"/>
        <modular-jar module="jhotdraw.nanoxml"/>
        <modular-jar module="jhotdraw.samples.draw"/>
        <modular-jar module="jhotdraw.samples.misc"/>
        <modular-jar module="jhotdraw.samples.net"/>
        <modular-jar module="jhotdraw.samples.odg"/>
        <modular-jar module="jhotdraw.samples.pert"/>
        <modular-jar module="jhotdraw.samples.svg"/>
        <modular-jar module="jhotdraw.samples.teddy"/>
        <modular-jar module="nanoxml"/>
    </target>
    <target name="dist" depends="jar">
    </target>
    <target name="distsigned" depends="jar">
    </target>
    <target name="documentation" depends="init">
        <delete dir="${doc}"/>
        <mkdir dir="${doc}"/>
        <copy todir="${doc}">
            <fileset dir="${docsrc}"/>
        </copy>
    </target>
</project>
